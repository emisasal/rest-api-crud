{
  "name": "rest-api-crud",
  "version": "1.0.0",
  "description": "",
  "main": "api/app.ts",
  "scripts": {
    "dev": "dotenvx run --env-file=.env.development --env-file=.env -- nodemon api/app.ts",
    "build": "dotenvx run --env-file=.env.development --env-file=.env -- npx tsc",
    "start": "node dist/app.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "rest",
    "api",
    "crud"
  ],
  "author": "Emiliano Sasal",
  "license": "ISC",
  "dependencies": {
    "@dotenvx/dotenvx": "^0.30.0",
    "@prisma/client": "^5.12.1",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.12.5",
    "nodemon": "^3.1.0",
    "prisma": "^5.12.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.4"
  }
}
